Remember that conventionally, when describing examples grous in RSpec with the "describe" keyword, 
the string argument for instance methods are written as “#method”, 
while string arguments for class methods are written as “.method”.
